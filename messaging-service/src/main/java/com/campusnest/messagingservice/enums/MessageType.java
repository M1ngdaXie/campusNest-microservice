package com.campusnest.messagingservice.enums;

public enum MessageType {
    TEXT,           // Regular text message
    SYSTEM,         // System-generated message (e.g., "User started conversation")
    IMAGE,          // Image message (future feature)
    LISTING_UPDATE  // Housing listing update notification
}